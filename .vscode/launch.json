{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch",
            "type": "cppvsdbg",
            "request": "launch",
            "program": "${workspaceRoot}/tri",
            "args": [],
            "cwd": "${workspaceRoot}",
            "console": "integratedTerminal"
        }
    ]
}